import { describe, it } from 'vitest';

describe('Nav', () => {
	it('should render a Nav', () => {
		// TODO: write tests
	});
});

describe('Header', () => {
	it('should render a Header', () => {
		// TODO: write tests
	});
});

describe('Meta', () => {
	it('should add correct meta tags to document', () => {
		// TODO: write tests
	});
});
