import { describe, it } from 'vitest';

describe('TaskForm', () => {
	it('should render a task form', () => {
		// TODO: write tests
	});
});

describe('TaskList', () => {
	it('should render a task list', () => {
		// TODO: write tests
	});
});
